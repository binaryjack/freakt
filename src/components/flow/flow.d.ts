interface IFlow {
    id: number
    name: string
    active: boolean
    dateOfBirth: Date
    order: number
}

interface IFlow2 {
    id: number
    name: string
    active: boolean
    dateOfBirth: Date
    order: number
}

interface IFlow3 {
    id: number
    name: string
    active: boolean
    dateOfBirth: Date
    order: number
}

interface IComponent2 {
    id: number
    name: string
    active: boolean
    dateOfBirth: Date
    order: number
    image: string
    elements: React.ReactNode
}
